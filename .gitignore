# Dependencies
node_modules/

# Astro cache
.astro/

# NOTE: dist/ IS committed. The Cloudflare project deploys committed static
# assets (no CI build step), so the built site must be in the repo. Regenerate
# with `npm run build` before committing.

# Legacy pack + Brand Book, regenerated by scripts/fold-legacy.mjs at build time
public/3d-process/
public/brand-guidelines/
public/book/

# OS / editor
.DS_Store
*.log
